#ifndef WRITESQUARES_H
#define WRITESQUARES_H

/**
 *  Fills array A of dimension Sz with integer squares.
 *  Pre:
 *        A points to an array of dimension Sz (or larger)
 *  Post:
 *        A[k] = k * k, for k = 0:Sz-1
 */
void WriteSquares(int* const A, int Sz);

#endif

